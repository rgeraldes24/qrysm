participants:
  - cl_type: qrysm
    cl_image: qrysm:local
    # cl_extra_params:
    #   - --subscribe-all-data-column-subnets
    #   - --target-peers=3
    count: 2
  - cl_type: qrysm
    cl_image: qrysm:local
    # cl_extra_params:
    #   - --target-peers=3
    count: 2
network_params:
  seconds_per_slot: 15
snooper_enabled: false
global_log_level: debug
additional_services:
  #- dora
  - prometheus_grafana
